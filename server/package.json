{
  "name": "@cashew/server",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "lint": "eslint --ext .ts .",
    "build": "yarn lint && tsc",
    "build:watch": "tsc --watch",
    "start": "ts-node src/index.ts"
  },
  "dependencies": {
    "@cashew/common": "*"
  },
  "devDependencies": {
    "ts-node": "^6.1.0",
    "@types/node": "^14.0.5",
    "typescript": "^3.9.3"
  }
}